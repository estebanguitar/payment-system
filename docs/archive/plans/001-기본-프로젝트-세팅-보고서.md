# 착수 중 산출물: 기본 프로젝트 세팅 보고서 (001)

- **작성일**: 2026-08-08
- **상태**: 완료 (SUCCESSFUL)
- **검증 결과**: `./gradlew test` (SUCCESSFUL - 0 Errors)

---

## 1. 프로젝트 세팅 개요

본 보고서는 결제 및 지갑 백엔드 시스템 구축을 위한 착수 전 기본 프레임워크 및 인프라 세팅 완료 현황을 기록한다.

---

## 2. 세부 세팅 내역

### 2.1 Gradle 빌드 및 의존성 구성 (`build.gradle`)
* **Java 버전**: Java 17 (Amazon Corretto 17.0.13)
* **Spring Boot 버전**: 3.5.16
* **의존성 모듈**:
  - `spring-boot-starter-web` (Spring Web MVC)
  - `spring-boot-starter-data-jpa` (Spring Data JPA)
  - `spring-boot-starter-validation` (Bean Validation)
  - `spring-boot-starter-actuator` (헬스체크 및 릴리스 모니터링)
  - `flyway-core` (데이터베이스 DDL 마이그레이션)
  - `h2` (In-Memory Database)
  - `lombok` (개발 생산성)
  - `spring-boot-starter-test` (JUnit 5, Mockito)

### 2.2 애플리케이션 설정 (`application.yml`)
* H2 인메모리 DB 연결 (`jdbc:h2:mem:paymentdb;DB_CLOSE_DELAY=-1`)
* H2 콘솔 활성화 (`/h2-console`)
* JPA Hibernate `ddl-auto: validate` 및 Flyway 마이그레이션 연동
* AES-256 암호화 시크릿 키 주입 설정

### 2.3 Flyway 데이터베이스 DDL 마이그레이션 (`src/main/resources/db/migration/`)
* **`V1__init_schema.sql`**:
  - `customer` (고객/사용자 원장 테이블, customer_id UNIQUE)
  - `wallet` (지갑 원장 테이블, customer_id UNIQUE & FK `fk_wallet_customer`)
  - `wallet_transaction` (지갑 거래 이력 테이블)
  - `payment` (결제 원장 테이블, idempotency_key UNIQUE & FK `fk_payment_customer`)
  - `payment_cancel` (결제 취소 거래 테이블)
  - `pg_response_log` (외부 PG 응답 암호화 원문 로그 테이블)
  - `payment_outbox` (이벤트 유실 방지 아웃박스 테이블)
* **`V2__insert_sample_data.sql`**:
  - 초기 시드 고객 지갑 데이터 (`CUST-001`: 100,000원, `CUST-002`: 50,000원)

### 2.4 메인 애플리케이션 컴포넌트 (`PaymentSystemApplication.java`)
* `@EnableScheduling`: Outbox Poller 스케줄러 기능 지원
* `@EnableAsync`: 비동기 이벤트 핸들링 지원

---

## 3. 검증 결과

* **명령어**: `gradlew.bat test`
* **결과**: `BUILD SUCCESSFUL` (0 에러, Flyway DDL 및 JPA 엔티티 검증 통과)
> 보관 문서입니다. 현재 기준은 [최신 문서](../../current/README.md)를 확인합니다.
