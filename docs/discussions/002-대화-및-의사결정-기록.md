# 대화 및 의사결정 기록 (Discussion & Decision Log)

## 1. 개요
본 문서는 PM, 아키텍트, 테크리드 및 제3자가 프로젝트 진행 상황과 의사결정 맥락을 한눈에 이해할 수 있도록 대화 내용과 합의사항을 정리한 기록이다.

---

## 2. 세션 기록 (2026-08-08)

### 2.1 요구사항 검토 및 질의응답
- **요구사항 정의서 검토**: `docs/pre-initiation-deliverables/requirements-definition.md`에 명시된 Must 요구사항(결제 원장 생성, 가상 PG 연동, 지갑 잔액 차감/환불, 전액/부분 취소, 멱등성 처리, 이력 조회)을 확인.
- **핵심 기술 선택 질의응답**:
  1. **지갑 잔액 동시성 제어 방식**:
     - 질의: 낙관적 락 vs 비관적 락
     - 결정: **비관적 락 (`Pessimistic Lock - SELECT FOR UPDATE`)** 채택. 동시 결제/충전 시 잔액 초과 차감 및 음수 잔액 방지를 위한 정합성 보장.
  2. **가상 외부 PG 연동 방식**:
     - 질의: REST Fake Server vs Spring Bean 가상 PG Component
     - 결정: **Spring Bean 기반 가상 PG Client Component** 채택. 외부 승인, 거절, ERROR, 타임아웃을 유연하고 확정적으로 시뮬레이션.

### 2.2 산출물 문서 생성 및 디렉터리 구조 이관
- `docs/discussions/`: 대화 요약 및 의사결정 기록 보관
  - `001-작업-지침-및-협업-규칙.md`
  - `002-대화-및-의사결정-기록.md`
- `docs/pre-initiation-deliverables/`: 착수 전 산출물 보관
  - `001-결제-시스템-요구사항-정의서.md`
  - `002-아키텍처-정의서.md` (계층구조, 동시성 시퀀스, 멱등성 플로우, 상태전이도, Flyway DDL, Outbox 패턴)
  - `003-API-명세서.md`
  - `adr/001-동시성-제어-및-외부-PG-연동-전략.md`
  - `adr/002-이벤트-기반-트랜잭션-분리-전략.md`
  - `adr/003-아웃박스-패턴-및-마이그레이션-전략.md`: Outbox-Ready 아키텍처 및 Phase 2 확장 리스크 0% 전략
- `docs/in-progress-deliverables/`: 착수 중 산출물 보관
  - `001-기본-프로젝트-세팅-보고서.md`: Spring Boot 3.5.16, H2, Flyway DDL 6개 테이블, yml, 테스트 검증 완료 보고서
- `docs/completion-deliverables/`: 완료 보고 산출물 보관 디렉터리 생성

### 2.3 트랜잭션 경계 분리 및 아웃박스(Outbox) 아키텍처 의사결정
- **Kafka 미도입 사유**: 단일 백엔드/인메모리 H2 환경에서 Kafka 브로커 운영 복잡도, Debezium Outbox 패턴 구현 등 과도한 오버엔지니어링(Over-engineering) 방지.
- **Spring Event (`@TransactionalEventListener AFTER_COMMIT`) 채택 사유**: `PENDING` 저장 DB 트랜잭션이 성공적으로 Commit된 후 이벤트를 발송하여 외부 HTTP 호출 시 DB Connection Pool 고사를 원천 차단하고 결합도를 제거.
- **Outbox-Ready 패턴 채택 사유**: JVM Crash 시 메모리 이벤트 유실 방지 및 Phase 2 분산 환경 전환 시 결제 원장 코드 수정 리스크 0% 보장.

---

## 3. 세션 기록 (2026-08-09)

### 3.1 도메인 레이어 구현계획 수립 요청

- 사용자는 상세 아키텍처 정의서를 기준으로 도메인 레이어의 구현계획 작성을 요청했다.
- 요구사항 정의서, API 명세, ADR 0001~0003, Flyway 스키마, Lombok 코딩 컨벤션 및 현재 소스 구조를 함께 검토했다.
- 구현 범위를 엔티티, 열거형, 도메인 예외, 도메인 단위 테스트 및 JPA 매핑 테스트로 한정하고 Application·Presentation·Infrastructure 구현은 후속 단계로 분리했다.
- 코드 수준의 패키지·파일 구성, Aggregate 경계, 생성 팩터리, 상태 전이 메서드, 금액 불변식, 테스트 및 완료 기준을 `docs/in-progress-deliverables/002-도메인-레이어-구현계획.md`에 작성했다.
- 계획은 사용자 승인 대기 상태이며 승인 전에는 구현, 커밋, 푸시 및 PR 생성을 진행하지 않는다.

### 3.2 구현 전 확인이 필요한 정합성 항목

1. 요구사항의 “시스템 전체 멱등키 유일” 정책과 현재 테이블별 UNIQUE 제약 사이에 차이가 있어 별도 글로벌 멱등성 원장 도입 또는 요구사항 변경 결정이 필요하다.
2. 동일 결제의 동시 부분 취소 시 누적 취소액 초과를 막기 위해 원 결제에도 비관적 쓰기 잠금을 적용할 필요가 있다.
3. 취소 실패 사유가 별도 enum으로 정의되지 않아 결제 실패 사유 재사용 여부를 구현 전 확정해야 한다.

### 3.3 Flyway DDL 컬럼 설명 추가

- 사용자는 스키마 DDL의 각 컬럼에 설명을 추가하도록 요청했다.
- `V1__init_schema.sql`의 7개 테이블 전체 컬럼에 H2 호환 `COMMENT ON COLUMN` 문법으로 한글 설명을 추가하는 계획을 제안했고 사용자가 승인했다.
- 현재 1페이즈는 재시작 시 초기화되는 H2 인메모리 DB를 사용하므로 별도 증분 마이그레이션을 생성하지 않고 초기 스키마 V1을 보완하기로 했다.
- 최초에는 별도 `COMMENT ON COLUMN` 문으로 작성했으나, 사용자가 테이블 정의를 읽는 즉시 설명을 확인할 수 있도록 요청하여 각 `CREATE TABLE`의 컬럼 정의에 인라인 `COMMENT`를 배치하는 방식으로 변경했다.

### 3.4 코드 주석 협업 규칙 추가

- 도메인 레이어 구현 중 사용자는 협업자가 코드 의도를 쉽게 파악할 수 있도록 클래스와 메서드에 간단한 설명 주석을 작성하도록 요청했다.
- `AGENTS.md`와 코딩 컨벤션에 클래스 책임 및 메서드 기능·주요 제약을 간결하게 문서화하는 규칙을 추가했다.
- 공개 API와 도메인 메서드는 Javadoc을 우선하고, 구현을 그대로 반복하는 불필요한 주석은 제외하기로 했다.

### 3.5 Swagger/OpenAPI 명세 요구사항 추가

- 사용자는 Swagger를 사용해 API 명세서를 작성하도록 요구사항 정의서 수정을 요청했다.
- Swagger UI/OpenAPI 3 기반의 명세 자동 생성과 개발 환경 호출 기능을 Must 요구사항 `API-002`로 추가했다.
- 요청·응답 모델, `Idempotency-Key`, 경로·쿼리 파라미터, 공통 응답 및 주요 오류 코드를 문서화하도록 인수 기준을 정의했다.
- 실제 `springdoc-openapi` 의존성 및 Controller 문서화는 후속 Presentation/API 계층 구현 범위로 분리했다.

### 3.6 도메인 오류 코드 및 메시지 표준화

- 사용자는 오류 코드와 메시지를 enum으로 정의해 여러 도메인 예외에서 재사용하도록 요청했다.
- `DomainErrorCode`에 업무 오류 코드와 표준 메시지를 함께 정의하고 `DomainException`이 해당 enum을 보유하도록 변경했다.
- 엔티티에 흩어진 문자열 오류 메시지와 일반 입력 예외를 표준 도메인 오류로 교체하고 코드·메시지 재사용 테스트를 추가했다.

### 3.7 도메인 레이어 구현 완료

- 사용자가 도메인 레이어 구현계획을 승인하여 `feature/domain-layer` 브랜치를 생성하고 구현을 진행했다.
- Customer, Wallet, WalletTransaction, Payment, PaymentCancel, PgResponseLog, PaymentOutbox 및 관련 enum·도메인 예외를 구현했다.
- Aggregate 간 연관관계는 식별자 참조로 유지하고, 공개 Setter 없이 도메인 메서드로 상태와 금액 불변식을 통제했다.
- 엔티티 필드에 `final`을 사용하지 않은 이유는 JPA 식별자 주입, 프록시·dirty checking 및 결제·잔액 상태 변경 생명주기와의 호환성 때문임을 사용자에게 설명했다.
- 전체 25개 테스트가 실패와 오류 없이 통과했으며 JPA/Flyway 매핑 및 DDL 컬럼 설명 메타데이터도 함께 검증했다.

### 3.8 JaCoCo 테스트 커버리지 리포트 적용

- 사용자는 테스트 결과와 코드 커버리지를 HTML로 확인할 수 있도록 JaCoCo 적용을 요청했다.
- 요구사항 정의서에 JaCoCo HTML 리포트 생성을 Must 품질 요구사항 `QUA-001`로 추가했다.
- Gradle `test` 완료 후 `jacocoTestReport`가 자동 실행되고 HTML 및 XML 리포트를 생성하도록 구성했다.
- 1페이즈에서는 커버리지 가시화에 사용하고 커버리지 임계치에 따른 빌드 실패 정책은 2페이즈에서 결정하기로 했다.
- `gradlew.bat clean test jacocoTestReport` 검증 결과 25개 테스트가 모두 통과했으며, 전체 기준 라인 88.18%, 브랜치 64.55%, 메서드 97.06%, 클래스 100% 커버리지를 확인했다.

### 3.9 GitHub 반영 요청

- 사용자는 승인된 도메인 레이어 구현과 관련 문서 변경을 커밋하고 원격 저장소에 푸시한 뒤 `main` 브랜치 대상 PR을 생성하도록 요청했다.
- Git 작성자는 저장소에 설정된 사용자 정보(`estebanguitar`, `esteban1009@naver.com`)를 사용한다.
- 구현 변경을 `3c36de7`(`feat: 도메인 레이어 구현`)로 커밋하고 `origin/feature/domain-layer`에 푸시했다.
- `main` 브랜치 병합을 위한 GitHub PR #1을 생성했다: `https://github.com/estebanguitar/payment-system/pull/1`
