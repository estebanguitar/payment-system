spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:/data/paymentdb;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    show-sql: false

payment:
  outbox:
    enabled: false
